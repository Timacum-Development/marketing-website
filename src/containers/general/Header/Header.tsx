import React from 'react';
import { NavLink } from 'react-router-dom';

// Assets
import Nav from '../Nav';

const Header = () => {
	return (
		<div>
			<nav>
				<Nav />
			</nav>
		</div>
	);
};

export default Header;
